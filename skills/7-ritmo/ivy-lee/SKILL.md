---
name: ivy-lee
description: "Ivy Lee method — at most six items, in order, and the second does not start until the first is closed. Use when writing a session or day plan, when a list is long enough to skim, or when several items are half-open at once."
---

# Ivy Lee

Six items, ranked, worked strictly in order, unfinished ones carried to
tomorrow's six. The limit is the method: a list you cannot hold in your
head is a list you will skim, and a list you skim gets its items opened
in parallel.

## Steps

1. **Cut to six.** Rank by importance; everything below six goes to a
   holding list, not to the plan. If six feels too few, the items are too
   small — merge them.
   _Done when_: the plan has at most six lines, each with its check.

2. **One at a time.** Work the first to its check before reading the
   second. If blocked, write the block and move to the second — the first
   stays first tomorrow.
   _Done when_: each item is closed, blocked-with-reason, or not started.

3. **Carry, don't grow.** At close, unfinished items head tomorrow's list;
   new items compete for the remaining slots.
   _Done when_: tomorrow's six are written, unfinished first.

## Example

Illustrative scenario; paths, commands, and results below are examples, not
artifacts or measurements from this repository.

**Request:** “I keep switching among nine half-finished tasks. Order tomorrow.”

Use concrete checks, not “work on” labels. This three-item plan is enough:

```text
1. Restore search indexing — missing-page fixture passes.
2. Finish mobile navigation — keyboard and touch walkthroughs pass.
3. Prepare internal setup docs — link check passes and local preview is reviewed.
Holding list: remaining six requests, with their existing issue locations.
```

Tomorrow, indexing is blocked on a missing sample file. Record that exact
blocker before moving to navigation; do not alternate between them every
few minutes. At close, navigation is done and docs have not started. The
following plan begins with indexing, then docs; new requests compete for
at most four remaining slots.

Six is a ceiling, not a target. If the sample arrives while navigation is
being finished, close navigation's check before reopening indexing. An
arrival notification is not a reason to leave another item half-done.
